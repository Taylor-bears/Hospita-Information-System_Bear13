# 医疗管理系统 - 项目架构说明


```
medical-system/
├── backend/                    # 🔧 后端核心目录 (在此运行后端)
│   ├── main.py                # FastAPI 主应用入口
│   ├── database.py            # 数据库连接配置
│   ├── models.py              # SQLAlchemy 数据模型
│   ├── schemas.py             # Pydantic 请求/响应模型
│   ├── requirements.txt       # Python 依赖包列表
│   └── init_db.sql           # 数据库初始化脚本
│
├── frontend/                   # 🎨 前端核心目录 (在此运行前端)
│   ├── src/
│   │   ├── main.jsx          # React 应用入口
│   │   ├── App.jsx           # 路由配置
│   │   ├── index.css         # 全局样式
│   │   ├── login/            # 登录注册页面组件
│   │   │   ├── Login.jsx
│   │   │   └── Register.jsx
│   │   ├── administrator/    # 管理员模块页面组件
│   │   │   ├── AdminLayout.jsx           # 管理员布局
│   │   │   ├── UserManagement.jsx        # 用户管理
│   │   │   ├── AppointmentManagement.jsx # 预约管理
│   │   │   ├── RecordManagement.jsx      # 病历管理
│   │   │   ├── PharmacyManagement.jsx    # 药房管理
│   │   │   └── ReviewManagement.jsx      # 医生评价
│   │   └── pages/            # 其他页面
│   ├── index.html            # HTML 入口文件
│   ├── package.json          # npm 依赖配置
│   └── vite.config.js        # Vite 构建配置
│
├── administrator/              # 管理员模块后端路由
│   └── backend/
│       └── routes.py          # 管理员相关 API 路由
│
├── login/                      # 登录模块后端路由
│   └── backend/
│       └── routes.py          # 登录注册 API 路由
│
└── README.md                   # 项目说明文档
```

## 启动方式

### 1️⃣ 启动后端 (FastAPI)
```powershell
# 激活 conda 环境
conda activate D:\SOFTWARE\Miniconda\envs\software_design

# 进入后端目录
cd medical-system/backend

# 启动后端服务
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```
**后端运行地址**: http://127.0.0.1:8000

### 2️⃣ 启动前端 (React + Vite)
```powershell
# 进入前端目录
cd medical-system/frontend

# 启动前端服务
npm run dev
```
**前端运行地址**: http://localhost:5173

## 架构设计

### 模块化后端架构
- **backend/main.py**: 主应用,聚合所有模块路由
- **login/backend/routes.py**: 处理用户登录、注册逻辑
- **administrator/backend/routes.py**: 处理管理员所有功能 API

### 前端单页应用 (SPA)
- 使用 **React Router** 实现路由管理
- **Ant Design** 作为 UI 组件库
- **Axios** 处理 HTTP 请求
- **Vite** 作为构建工具(快速热更新)

## 默认账号

- **管理员账号**: `13800138000`
- **管理员密码**: `admin`
- **bear的账号**：`神秘数字`

## 📊 数据库配置

修改 `backend/database.py` 中的数据库连接信息:
```python
DATABASE_URL = "mysql+pymysql://用户名:密码@localhost:3306/数据库名"
```
大家根据自己的MySQL修改即可，后续统一数据，一并导入就好了

## ✅ 已实现功能

### 登录注册模块
- ✅ 用户注册 (普通用户/医生)
- ✅ 医生注册需审核
- ✅ 角色分类登录

### 管理员模块
- ✅ 用户管理 (查看/添加/删除)
- ✅ 医生管理 (查看/添加/删除/审核)
- ✅ 管理员管理 (查看/添加/删除,带最后管理员保护)
- ⏳ 预约管理 (待开发)
- ⏳ 病历管理 (待开发)
- ⏳ 药房管理 (待开发)
- ⏳ 医生评价 (待开发)

## 🔧 技术栈（方便写文档）

**后端**:
- FastAPI 0.122.0
- SQLAlchemy 2.0.44
- PyMySQL 1.1.2
- Passlib + bcrypt 3.2.2

**前端**:
- React 18
- Ant Design 5.x
- React Router 6
- Axios
- Vite 5.4.21

---
